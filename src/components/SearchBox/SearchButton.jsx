import { SearchIcon } from "../../svg/SearchIcon";

const SearchButton = () => {
  return (
    <button className="search-btn">
      <SearchIcon />
    </button>
  );
};

export default SearchButton;
