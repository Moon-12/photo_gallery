import { SearchIcon } from "../../icons/SearchIcon";

const SearchButton = () => {
  return (
    <button className="search-btn">
      <SearchIcon />
    </button>
  );
};

export default SearchButton;
